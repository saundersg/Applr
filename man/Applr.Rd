% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Applr-package.R
\docType{package}
\name{Applr}
\alias{Applr}
\alias{Applr-package}
\title{Applr: Tools for Visualizing and Explaining Linear Models}
\description{
Applr is a small R package that makes it easy to visualize slices of
higher-dimensional linear models and to present model equations clearly.
It includes base R and ggplot2 helpers, plus an interactive 3D view for
models with two predictors.
}
\details{
Core capabilities:
\itemize{
\item 2D slices of multi-variable models in base graphics
\item ggplot2 layer for plotting model slices across facets
\item Interactive 3D scatter with fitted surface (two predictors)
\item Quick text/LaTeX equations from lm() objects
}
}
\section{Main functions}{

\itemize{
\item Base graphics: \code{\link{slice_2d}}, \code{\link{add_slice_2d}}, \code{\link{drawit}}
\item ggplot2 layer: \code{\link{geom_slice}} (built on \code{\link{StatSlice}} and \code{\link{GeomSlice}})
\item 3D visualization: \code{\link{scatter_3d}}
\item Equations: \code{\link{lm_equation}}, \code{\link{lm_latex}}
\item Theme: \code{\link{theme_lc}}
}
}

\section{Quick start}{

Fit a model and plot a slice:
\preformatted{
library(Applr)
model <- lm(mpg ~ disp + hp, data = mtcars)
slice_2d(model, xaxis = "disp")
}

ggplot2 layer with facets:
\preformatted{
library(ggplot2)
model <- lm(mpg ~ disp + hp + cyl, data = mtcars)
ggplot(mtcars, aes(disp, mpg)) +
  geom_point() +
  facet_wrap(~cyl) +
  geom_slice(model = model, predict_vars = list(hp = 110))
}
}

\seealso{
\itemize{
\item Function help pages: \code{?slice_2d}, \code{?geom_slice}, \code{?scatter_3d}, \code{?lm_equation}, \code{?lm_latex}, \code{?theme_lc}
\item The package README for a brief tour
}
}
\author{
\strong{Maintainer}: Cameron McClellan \email{cameron.m.mcclellan@gmail.com}

}
