% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.lm.R
\name{autoplot.lm}
\alias{autoplot.lm}
\title{Automatically plot a linear model}
\usage{
\method{autoplot}{lm}(
  object,
  mapping = NULL,
  type = c("2d", "3d"),
  summary = TRUE,
  interval = "confidence",
  ...
)
}
\arguments{
\item{object}{A linear model fitted by \code{\link[=lm]{lm()}} \strong{with a \code{data} argument} —
the data is recovered from the model, so \code{lm(y ~ x, data = your_data)}
works but \code{lm(your_data$y ~ your_data$x)} does not.}

\item{mapping}{Aesthetics created with \code{\link[ggplot2:aes]{ggplot2::aes()}}, passed to
\code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} — for example \code{mapping = aes(color = factor(cyl))}.
They resolve against the model's full data frame, so a column the model
never mentions (such as \code{cyl} above) still works. An entry named \code{x}
chooses the x-axis (\code{mapping = aes(x = hp)}), overriding the default first
numeric predictor; an entry named \code{y} overrides the response axis.}

\item{type}{\code{"2d"} (default) for a \code{\link[=geom_slice]{geom_slice()}} ggplot, or \code{"3d"} for an
interactive \code{\link[=scatter_3d]{scatter_3d()}} surface (needs exactly two numeric predictors).}

\item{summary}{Whether to print \code{summary(object)} to the console before
returning the plot (default \code{TRUE}). Printing it up front means you can
refit the model and inspect its coefficients from inside the same
\code{autoplot()} call; pass \code{summary = FALSE} to suppress it.}

\item{interval}{The ribbon drawn around the slice line, passed to
\code{\link[=geom_slice]{geom_slice()}}: \code{"confidence"} (default) for the mean's confidence
interval, \code{"prediction"} for a single new observation's interval, or
\code{"none"} for a bare line. Ignored when \code{type = "3d"}.}

\item{...}{Passed on to \code{\link[=geom_slice]{geom_slice()}} — for example
\code{predict_vars = list(hp = 110)} to choose the slice, or fixed aesthetics
such as \code{color = "red"} — or to \code{\link[=scatter_3d]{scatter_3d()}} when \code{type = "3d"}.}
}
\value{
A ggplot of the model's data with a slice of the model drawn
through it, or — when \code{type = "3d"} — a plotly surface from \code{\link[=scatter_3d]{scatter_3d()}}.
}
\description{
\code{autoplot()} turns a fitted \code{\link[=lm]{lm()}} into a complete ggplot in one call: the
data the model was fitted to as a scatter, with a \code{\link[=geom_slice]{geom_slice()}} prediction
line — and, by default, its confidence ribbon — through it. The result is a
regular ggplot, so it can be extended with \code{+} as usual (labels, scales,
more layers).
}
\details{
Pass \code{type = "3d"} for a model with exactly two numeric predictors to get an
interactive \code{\link[=scatter_3d]{scatter_3d()}} surface instead of the 2-D slice; \code{...} is then
forwarded to \code{\link[=scatter_3d]{scatter_3d()}} (e.g. \code{n}, \code{colors}).

The model's \emph{first numeric} predictor goes on the x-axis (override with
\code{mapping = aes(x = ...)}) and the raw response variable goes on the y-axis.
A predictor that groups into a few discrete lines rather than varying
continuously — non-numeric, or numeric with at most five distinct values
(like \code{cyl}'s 4/6/8) — is worth showing, not holding at one value: the first
such predictor not already on the plot becomes \code{aes(color = ...)} (one slice
line per level) and a second one facets the plot. Everything past that
is handled by \code{\link[=geom_slice]{geom_slice()}} and reported on the console: predictors not
visible on the plot are imputed (mean for numeric, most common value for
factor/character), and a transformed response such as \code{lm(log(y) ~ x)} is
automatically back-transformed to match the raw y-axis.
}
\examples{
library(ggplot2)

# A complete plot from a model in one call
autoplot(lm(mpg ~ disp, data = mtcars))

# Predictors not on the plot are imputed (a message says how)
autoplot(lm(mpg ~ disp + hp, data = mtcars))

# Choose which predictor goes on the x-axis
autoplot(lm(mpg ~ disp + hp, data = mtcars), mapping = aes(x = hp))

# Transformed response, back-transformed onto the raw mpg axis
autoplot(lm(log(mpg) ~ disp, data = mtcars))

# Options pass through to geom_slice(); the result is a normal ggplot
autoplot(lm(mpg ~ disp + hp, data = mtcars),
         predict_vars = list(hp = 110), interval = "prediction") +
  labs(title = "Slice at hp = 110")

# Extra aesthetics for the scatter (mapping goes to ggplot())
autoplot(lm(mpg ~ hp + factor(cyl), data = mtcars),
         mapping = aes(color = factor(cyl)))

# An interactive 3-D surface for a two-predictor model
if (interactive()) {
  autoplot(lm(mpg ~ disp + hp, data = mtcars), type = "3d")
}

}
\seealso{
\code{\link[=geom_slice]{geom_slice()}}, which draws the line and handles everything
slice-shaped; \code{\link[=scatter_3d]{scatter_3d()}} for the interactive surface; and
\code{\link[=geom_slice_subtitle]{geom_slice_subtitle()}} / \code{\link[=geom_slice_text]{geom_slice_text()}} to annotate the result.
}
