% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated.R
\name{drawit}
\alias{drawit}
\title{Deprecated: use slice_2d() instead}
\usage{
drawit(model, xaxis, ...)
}
\arguments{
\item{model}{A linear model fit with \code{\link[stats:lm]{stats::lm()}}.}

\item{xaxis}{The primary explanatory variable to vary along the x-axis.}

\item{...}{Graphical parameters forwarded to \code{\link[=slice_2d]{slice_2d()}} (e.g. \code{col}, \code{lty}).}
}
\value{
A 2D base-R plot of a model slice (called for its side effect).
}
\description{
\code{drawit()} drew a single base-R slice of an \code{lm()} and required the caller
to name a value for every other predictor. That job is now done by
\code{\link[=slice_2d]{slice_2d()}}, which imputes unspecified predictors (mean for numeric, most
common level for factors) instead of demanding them. \code{drawit()} is kept as a
deprecated shim: it warns and forwards to \code{\link[=slice_2d]{slice_2d()}}, mapping \code{xaxis} to
\code{x_axis} and passing graphical parameters (\code{col}, \code{lty}, ...) straight
through. It will be removed in a future release.
}
\seealso{
\code{\link[=slice_2d]{slice_2d()}}
}
\keyword{internal}
