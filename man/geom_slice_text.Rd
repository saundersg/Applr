% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_slice_text.R
\name{geom_slice_text}
\alias{geom_slice_text}
\title{Label the lines drawn by geom_slice()}
\usage{
geom_slice_text(
  style = "variable",
  location = "right",
  offset = 5,
  hjust = NULL,
  vjust = NULL,
  color = NULL,
  expand = TRUE
)
}
\arguments{
\item{style}{How to write the labels: \code{"variable"} (default) writes
\code{"x2: 0"}; \code{"value"} writes the bare \code{"0"}; \code{"legend"} writes bare values
plus a key in the panel's top-right corner naming the variables
(\code{"labels: x2; x3"}).}

\item{location}{Which end of the line to label: \code{"right"} (default) or
\code{"left"}.}

\item{offset}{Gap between the line end and the label edge, in points
(default 5). The gap is constant regardless of label text, axis range,
or plot size.}

\item{hjust, vjust}{Manual text justification. Default \code{NULL} anchors the
label at the line end, displaced by \code{offset}; setting \code{hjust} yourself
disables the automatic offset entirely.}

\item{color}{Label color. Default \code{NULL} inherits each line's color.}

\item{expand}{If \code{TRUE} (default), widen the x-range on the label side so
the labels fit (plus y-headroom for the \code{"legend"} key). \code{FALSE} leaves
the scales alone. Only the expansion is touched: a \verb{scale_x_*()} you set
yourself keeps its limits, breaks, name and transform, and an expansion
you set on the side away from the labels is kept as well. The room reserved is the widest label's drawn width
measured against the panel's own width, taken from the graphics device
open when the layer is added; a plot re-sized much narrower afterwards
may still clip. Labels needing more than half the panel are clipped with
a warning rather than crowding out the data.}
}
\value{
An object that adds the label layers when added to a ggplot.
}
\description{
\code{geom_slice_text()} writes a text label at the end of each line drawn by
the plot's \code{\link[=geom_slice]{geom_slice()}} layers. Multi-value \code{predict_vars} draw visually
identical lines with nothing distinguishing them; labels fix that, and
double as a legend replacement for grouping aesthetics.
}
\details{
It takes no \code{model} or \code{predict_vars} — everything is borrowed from the
plot's existing \code{geom_slice()} layers, so add it \emph{after} them. Labels
describe the \code{predict_vars} values (\code{"x2: 0"}, joined with \code{"; "} when
several variables are crossed); when the slice layer has no \code{predict_vars},
labels describe the grouping aesthetic instead (\code{"g: A"}). Each label
inherits its line's color.
}
\examples{
library(ggplot2)

# Two visually identical lines, told apart by their labels
model <- lm(mpg ~ disp + hp, data = mtcars)
ggplot(mtcars, aes(disp, mpg)) +
  geom_point() +
  geom_slice(model, predict_vars = list(hp = c(66, 335))) +
  geom_slice_text()

# A projection band, labeled on each edge
ggplot(mtcars, aes(disp, mpg)) +
  geom_point() +
  geom_slice(model, band = TRUE) +
  geom_slice_text()

}
\seealso{
\code{\link[=geom_slice]{geom_slice()}} for the layers being labeled;
\code{\link[=geom_slice_subtitle]{geom_slice_subtitle()}} / \code{\link[=geom_slice_caption]{geom_slice_caption()}} to describe the slice
in the plot's text instead.
}
