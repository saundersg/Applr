% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_slice_subtitle.R
\name{geom_slice_subtitle}
\alias{geom_slice_subtitle}
\title{Describe the lines drawn by geom_slice() in the plot subtitle}
\usage{
geom_slice_subtitle(model = TRUE, prepend = "", append = "", ..., wrap = TRUE)
}
\arguments{
\item{model}{If \code{TRUE} (default), the subtitle's first line is the model
equation; \code{FALSE} drops it, leaving only the held-values line.}

\item{prepend, append}{Plain strings pasted before the first line and after
the last line of the default subtitle.}

\item{...}{Passed to \code{\link[=lm_equation]{lm_equation()}} when the equation is written, under
its own argument names — chiefly \code{style}, which controls how factor terms
are labelled.

Left unset, \code{style} is chosen to fit: with \code{wrap} on, the equation is
tried spelled out (\code{4.09*(Species="setosa")}) and briefly
(\verb{4.09*[setosa]}), each with and without the hanging indent, preferring
the spelled-out form and giving up the indent last. The first that fits
wins, and a message says so if the brief form did. Because the indent
goes last, a narrower plot can bring the full names \emph{back}. The brief
form is never offered when two terms would shorten to the same label — a
bare \verb{[High]} from two different factors no longer says which it meant.

Naming \code{style} yourself pins it and switches all of that off. Pin it (or
give \code{wrap} a width) when the annotation must come out the same whatever
size device it is drawn on.}

\item{wrap}{Controls wrapping of lines too long to fit. \code{TRUE} (default)
measures how much room the subtitle has in this plot on the current
graphics device and breaks long lines to fit, between terms only, with
continuation lines hanging under the right-hand side of the equal sign.
\code{FALSE} leaves the text as one line per item, however far it runs off the
plot. A number is a width in inches to wrap to instead of measuring —
useful when the plot will be drawn at a size other than the current
device, as with \code{ggsave(width = )}.}
}
\value{
An object that sets the plot subtitle when added to a ggplot.
}
\description{
\code{geom_slice_subtitle()} fills the plot subtitle with a description of the
plot's \code{\link[=geom_slice]{geom_slice()}} layers: the model equation (\code{\link[=lm_equation]{lm_equation()}} style) on
the first line, and the held values the plot does not otherwise show on the
second (\code{"held at: x2 = 2.507"}, formatted like \code{geom_slice()}'s console
messages).
}
\details{
It takes no \code{model} or \code{predict_vars} — everything is borrowed from the
plot's existing \code{geom_slice()} layers, so add it \emph{after} them (and after
any \code{\link[=geom_slice_text]{geom_slice_text()}}). The held-values line is conscious of what the
plot already labels: variables labeled by \code{geom_slice_text()}, pinned by a
grouping aesthetic (the legend covers those), or pinned by faceting are
left out. User-chosen \code{predict_vars} values and imputed defaults are
treated the same — both are unlabeled held values. When nothing is held,
the line is dropped entirely.
}
\examples{
library(ggplot2)

# Subtitle reports the model equation and that hp is held at its mean
model <- lm(mpg ~ disp + hp, data = mtcars)
ggplot(mtcars, aes(disp, mpg)) +
  geom_point() +
  geom_slice(model) +
  geom_slice_subtitle()

}
\seealso{
\code{\link[=geom_slice]{geom_slice()}} for the layers being described;
\code{\link[=geom_slice_caption]{geom_slice_caption()}} for the same text in the caption;
\code{\link[=geom_slice_text]{geom_slice_text()}} to label the lines themselves.
}
