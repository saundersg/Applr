Message: The first predictor `Species` is not numeric - used `Petal.Length` for the x-axis.
    To choose the x-axis, use 'mapping = aes(x = Petal.Length)'. 
Message: Coloured the plot by `Species` - one slice line per level.
    To choose the colour, use 'mapping = aes(color = ...)'. 
